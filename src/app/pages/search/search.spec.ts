import { AppSearchPageComponent } from './search';

describe('AppSearchPageComponent', () => {
  let component: AppSearchPageComponent;

  beforeEach(() => {
    component = new AppSearchPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
